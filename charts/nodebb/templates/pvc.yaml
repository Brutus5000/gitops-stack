apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodebb-pvc
  labels:
    app: nodebb
spec:
  storageClassName: zfs-localpv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
